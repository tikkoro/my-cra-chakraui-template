import { Box } from '@chakra-ui/react'

type {{ properCase name }}Props = {
    sample: string;
};

const {{ properCase name }}: React.FC<{{ properCase name }}Props> = ({ sample }: {{ properCase name }}Props) => {
    const test = 'hello';

    return (
        <Box>
            {sample}
        </Box>
    );
};

export default {{ properCase name }};
